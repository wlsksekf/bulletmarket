server {
    listen 80;
    server_name bulletmarket.duckdns.org;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name bulletmarket.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/drawlink.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/drawlink.duckdns.org/privkey.pem;

    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # 도커 내부 DNS 지정
    resolver 127.0.0.11 valid=30s;

    location / {
        set $bm_front "http://bulletmarket-frontend:80";
        proxy_pass $bm_front;
    }

    location /api/ {
        set $bm_back "http://bulletmarket-backend:8080";
        proxy_pass $bm_back;
    }

    location /ws/ {
        set $bm_ws "http://bulletmarket-backend:8080";
        proxy_pass $bm_ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_buffering off;
    }
}